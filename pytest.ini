[pytest]
# Fail tests that hang or run unexpectedly long.
# Override per-test with: @pytest.mark.timeout(10)
# Override globally with: pytest --timeout=0 (disable) or --timeout=10
timeout = 2
timeout_method = signal
